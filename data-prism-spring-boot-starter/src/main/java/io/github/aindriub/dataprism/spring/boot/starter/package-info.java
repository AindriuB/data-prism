/**
 * A dependency-only aggregator: adding {@code data-prism-spring-boot-starter}
 * pulls in {@code data-prism-spring-boot-autoconfigure} and the Spring Boot
 * web and OAuth2 resource server starters it needs to expose the protected
 * HTTP MCP endpoint. This module carries no code of its own.
 */
package io.github.aindriub.dataprism.spring.boot.starter;
