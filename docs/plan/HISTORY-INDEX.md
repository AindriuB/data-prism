# History index

One row per `## YYYY-MM-DD` entry in `HISTORY.md`, newest first. Only `scribe`
edits this file, and it writes the row in the same commit as the entry.

`HISTORY.md` grows without bound and is the single largest file a session can
accidentally load. This index exists so `planner` can answer "has this been
built before, and where do I read about it?" without opening it. Rows are taken
from `HISTORY.md`'s own headings rather than re-summarized, so the index can be
checked against its source by eye.

Read a full entry by grepping the exact string in the **Heading** column:

```
grep -n '<heading text>' docs/plan/HISTORY.md
```

Then read that section only. A stale index is worse than none — an entry with
no row is one `planner` cannot find, and will re-plan.

| Date | Task IDs | Summary | Heading (grep this exact string) |
|---|---|---|---|
| 2026-09-24 | — | Corrects canonical description D's "redacts or refuses anything unclassified" to "refuses anything unclassified" — both shipped profiles use `FAIL_REQUEST` only, no redaction path for unclassified fields. Fixed everywhere D or a close paraphrase appears: `README.md`, `pom.xml`, `CITATION.cff`, mkdocs `site_description`/`llms.txt`, `.github/workflows/pages.yml`'s D check, the discoverability spec's D definition (+ dated note), four `docs/plan/outreach/*.md` drafts, and the two use-case pages' "redacted or the call is refused outright" phrasing. Left alone: classified-field redaction wording (still accurate), tagline T, `server.json`, OCI image labels (none contain D), and the GitHub About text (owner action via `gh repo edit`). Standalone fix on `site-polish`, not merged to `main`. | `## 2026-09-24 — Corrects canonical description D's "redacts or refuses" to "refuses"` |
| 2026-09-24 | — | Corrects the released `CHANGELOG.md` 0.3.0 `AuditChainVerifier` "Added" and "Not changed" bullets, which attached "caught even on that chain's own last record" to deletions as well as edits. Per `docs/audit.md`, only edits are caught anywhere including the last record; a deletion is caught only when a later record follows it — both bullets now say so, and `[Unreleased]` gained a `### Changed` note recording the correction (renders open first, "[Unreleased] · 1 changed"). Repo-wide grep for "edit or deletion"/"last record"/"tamper" found no other CHANGELOG hit and no identical overclaim in `README.md`, `docs/faq.md` or `docs/index.md`. Standalone fix on `site-polish`, not merged to `main`. | `## 2026-09-24 — Corrects the released 0.3.0 \`AuditChainVerifier\` CHANGELOG wording` |
| 2026-09-24 | — | Corrects `docs/tools.md:120`'s `entity` response-field row, which said unclassified values are "dropped" though the shipped `DEFAULT`/`STRICT` profiles both set `unclassified: FAIL_REQUEST` (refuses the whole response). Now: real values pseudonymised, sensitive values redacted or removed per classification, and an unclassified field refuses the whole response under the shipped profiles — matching `docs/configuration.md`'s wording. Traced through `PrivacyProfiles`/`ProfilePrivacyPolicyResolver`/`privacy-profiles-default.yaml`: `DROP_AND_WARN` exists in `UnclassifiedBehaviour` but no bundled profile selects it and no `dataprism.*` property loads a custom profile file, so it is unreachable today. Repo-wide grep found no other occurrence of the same claim. Standalone one-line fix on `site-polish`, not merged to `main`. | `## 2026-09-24 — Corrects \`docs/tools.md:120\`'s "unclassified values dropped" claim` |
| 2026-09-24 | 88 | Draws five Mermaid diagrams (system overview, one `get_entity_context` call, pseudonym generation, fail-closed field decisions, the audit chain) under `docs-site/diagrams/*.mmd`, rendered by `render.sh` via a digest-pinned `minlag/mermaid-cli` run `--network none` as the caller's uid to byte-stable, padded/bordered SVGs in `docs/assets/diagrams/`. Each is embedded insert-only in the doc that already covers it (`architecture.md`, `tools.md` x2, `configuration.md`, `audit.md`) — one intro sentence plus a linked image that opens full size. New `docs-site/hooks/check_diagrams.py` enforces `.mmd`/`.svg` pairing, page references, alt text and no off-w3.org/script/`@import` content; `docs-site/diagrams/README.md` traces every node/edge to a code or doc line. Two attempts: attempt 1 passed the tester but was illegible at page width (wide `flowchart LR` diagrams shrank to single-digit-pixel text in Material's ~688px column, caught only by screenshot) and the reviewer found diagram 5 overclaiming what the audit chain detects (attaching "including the last record" to deletions as well as edits) plus a bare white background on dark pages; attempt 2 redrew both wide diagrams top-to-bottom, fixed diagram 5's wording to `audit.md`'s own phrasing, and added SVG padding/border. PASS + APPROVE on attempt 2, merged onto local `site-polish` (not `main`). Retired task files' historical citations shift (not edited): `architecture.md:106-155` (task 12) now ends at 159, `:135-141` (task 13)'s 136-141 is now 140-145, and `HISTORY.md`'s own already-stale `architecture.md:156-159` citation shifts a further 4 lines; no live `PLAN.md` citation moved. Leaves `docs/tools.md:120`'s "unclassified values dropped" (contradicts `FAIL_REQUEST`) unblocked but not fixed here. | `## 2026-09-24 — Task 88: draw five concept diagrams and embed each in context` |
| 2026-09-24 | 89 | Opens `docs/developer-guide/index.md` (replacing task 85's stub) covering the extension points — `DataSourceAdapter`, `IdentityResolver`, `AuditSink` and the classification annotations — each linked to its source on GitHub and its `docs/extending.md` section, plus the learning path. Tutorial 1, `docs/developer-guide/write-an-adapter.md`, walks seven steps from an `@LlmExposedModel` record to a pseudonymised MCP response; every code/pom/YAML/Dockerfile block is pulled via `--8<--` from marked regions in `data-prism-quickstart-extension` and `docker/`, never hand-copied (`pom.xml` uses single-dash `-8<-`, since `--` is illegal in an XML comment). The tutorial's commands were run literally from a clean clone; the page quotes the real output (`customerName` pseudonymised, `email` redacted). New `docs-site/hooks/check_snippet_markers.py` reuses pymdownx 12.1's own section regex and fails on a missing, duplicate, unmatched or out-of-order marker — added because `pymdownx.snippets`' `check_paths` only guards missing *start* markers, silently reading to EOF on a missing end marker. `docs/extending.md` gains a five-line pointer. Two attempts: attempt 1 PASS but reviewer CHANGES for tutorial prose drifting from its own snippets (said the pom left core compile-scope right under a snippet showing it `provided`; overstated `@SensitiveData` as deciding the action rather than suggesting it) and for the missing-end-marker gap; both fixed in attempt 2 along with the marker-syntax fix. PASS + APPROVE on attempt 2, merged onto local `site-polish` (not `main`). Left as unscheduled follow-ups: the annotation-authority sentence still omits the no-profile-rule and `override: true` cases; `check_snippet_markers.py` would flag a future prose file quoting the marker syntax; its docstring should say to always read this repo's own `mkdocs.yml`. | `## 2026-09-24 — Task 89: start the developer guide with a tested adapter tutorial` |
| 2026-09-24 | 86 | Gives the docs site its own identity: the owner's prism mark as the header logo (`docs/assets/logo.svg`, byte-identical to the supplied `mark-dark.svg`; supplied files kept unchanged under `docs-site/logo/supplied/`), a script-derived favicon (SVG with a `prefers-color-scheme` switch, no incoming ray, thicker indigo bars, plus a 32px PNG via `docs-site/logo/make_favicon.py`), a dark-slate `#1e293b` header in both schemes with one indigo accent at two AA-tuned shades (`#4f46e5` light, `#818cf8` dark), new `docs-site/hooks/check_contrast.py` covering text/links/header/logo/both hero buttons in both schemes, a landing hero (tagline T, Quickstart and Developer guide buttons, three cards worded from the pages they link to) ahead of the existing README include, and `docs/stylesheets/extra.css` covering only palette/tables/code/cards/buttons. Two attempts: attempt 1 passed automated contrast checks but the reviewer's own screenshot review caught an invisible Developer-guide button in the slate scheme (light-indigo text on light-indigo), and copied the audit card's "caught even on the last record" wording from `CHANGELOG.md` rather than the more limited `docs/audit.md`; attempt 2 fixed both and extended `check_contrast.py` to both hero buttons in both schemes. PASS + APPROVE on attempt 2, merged onto local `site-polish` (not `main`). Left as an unscheduled follow-up: the hero buttons' hover state in slate is ~2.98:1, below AA, and `check_contrast.py` does not check hover. | `## 2026-09-24 — Task 86: give the docs site an identity` |
| 2026-09-24 | 87 | Renders each `CHANGELOG.md` release on `docs/changelog.md` as a `pymdownx.details` block, replacing task 85's no-op `changelog.py` stub: newest release open, the rest closed, an empty `[Unreleased]` dropped, each `<summary>` reading "version — date · N added · N changed · …" from a per-`###`-subsection top-level-item count (singular for a count of 1). Hides the page's own TOC via `page.meta["hide"]`. New `docs-site/hooks/check_changelog.py` fails, naming what's missing, unless every release marker/`<summary>` survives and every link-reference definition both stays anchored at top level and resolves. `CHANGELOG.md` untouched; only site side effect is `llms-full.txt`'s plain release-heading lines. Two attempts: attempt 1 PASS but reviewer CHANGES for a comment describing the counting rule backwards, a "1 behavioural changes" plural, no TOC suppression, and an unanchored link-definition check a details-indented definition could still pass; all four fixed in attempt 2. PASS + APPROVE on attempt 2, merged onto local `site-polish` (not `main`). | `## 2026-09-24 — Task 87: collapse the changelog page per release` |
| 2026-09-24 | 85 | Lays the shared `mkdocs.yml`/guard-script/CI plumbing every site-polish wave-1 task needs: `attr_list`, `md_in_html`, `pymdownx.snippets` (`check_paths`, `restrict_base_path`, `base_path: [data-prism-quickstart-extension, docker]`) and a `pymdown-extensions` pin; a "Developer guide" nav section with three stub pages; the `changelog.py` no-op hook task 87 owns; a third-party-script guard in `check_site.py` that allows exactly two `unpkg.com` strings inside Material's own bundle and fails on everything else (off-origin script/link/`@import`/`url(`, any `class="mermaid"`); `pages.yml` running every `check_*.py`; a merge-only `check_no_stub_pages.py` gated on `DP_REQUIRE_NO_STUBS`; and a CONTRIBUTING "## Docs site" section. Four attempts, all closing holes in the third-party guard (protocol-relative and backslash/tab URLs, unquoted/case-insensitive attributes, scheme-less bundle tokens, a too-broad `.map` exemption, minified `@import`, non-whitespace attribute separators); PASS + APPROVE on attempt 4, deliberately bounded — the out-of-scope obfuscation forms are named in a `check_site.py` comment. Merged onto local `site-polish` (not `main`). | `## 2026-09-24 — Task 85: shared site plumbing for the site-polish wave` |
| 2026-09-24 | 83 | Wires README and `server.json` to the now-live `https://aindriub.github.io/data-prism/` (task 82, deployed via PR #98). README's "## Documentation" splits into "User docs" (16 doc pages plus the site root, each linking the live URL and the repo file) and "Internal / project working docs" (not published). `server.json` gains `.websiteUrl`, effective at the next registry publish. Verified: site/sitemap/llms.txt all 200; README site-URL set equals the live sitemap's 17-page set exactly; `mcp-publisher validate` passes; the three `publish-mcp.yml` name extractions agree; `mcp-name` line and site-intro markers untouched. Two attempts: attempt 1 reviewer APPROVE but tester FAIL (missed the changelog and both agent-transport pages); attempt 2 added them. PASS + APPROVE on attempt 2. Base branch changed under it: `discoverability` reached `main` via PR #98 before this task closed, so it closed onto a new local `go-live-wiring` branch cut from `main` (not pushed, `main` untouched) carrying the `--no-ff` merge plus a close-out commit — reaching `main` is a separate, still-pending step. | `## 2026-09-24 — Task 83: README and \`server.json\` wired to the live docs site` |
| 2026-09-23 | 82 | Publishes `https://aindriub.github.io/data-prism/` via MkDocs Material (mkdocs 1.6.1, material 9.7.7, include-markdown 7.3.0, llmstxt 0.5.0, all pinned), built from the existing docs as the single source: 17 nav pages, `docs/index.md` (new) includes the README intro between the site-intro markers, `docs/changelog.md` (new) includes `CHANGELOG.md`, existing docs get no front matter (titles/descriptions from `docs-site/page-meta.yml`), internal docs excluded (`plan/`, `adr/`, `pack.md`, conventions/workflow/development-plan/design-review), OG/Twitter tags plus the existing social card on every page, home-page-only JSON-LD `SoftwareSourceCode` with no version, `llms.txt`/`llms-full.txt`, no analytics, `theme.font: false`. `docs-site/hooks/site.py` rewrites links leaving `docs/` to GitHub blob/tree URLs (raw for images) and fails the build if the target does not exist; `check_site.py` verifies excluded paths, sitemap-vs-nav, meta/canonical/social tags, JSON-LD, no fonts/analytics/robots.txt, and the llms files. `.github/workflows/pages.yml` builds strict + guards on PRs and `main`, deploys only on push to `main` via the `github-pages` environment; no branch-protection change, not a required check. Two attempts: attempt 1's site was correct but three of its own guards could not fail (sitemap checked against the same manifest it is built from, llms-leak searched for a URL the rewrite made impossible, link-rewrite never checked targets exist); attempt 2 fixed all three and proved each by planting the fault it now catches. PASS + APPROVE on attempt 2, diff confirmed limited to the owned files, merged `--no-ff` onto the local `discoverability` branch (not `main`), then a close-out commit. Four unscheduled follow-ups in `docs/plan/PLAN.md` "Found on task 82": the llms-leak check tests only the first ≥40-char line per excluded doc, a nested badge image gets a `blob` URL instead of `raw`, `?query`/percent-encoded links leaving `docs/` fail loudly, and MkDocs/Material's pins need re-checking before bumping (MkDocs 2.0 warning, Zensical migration path). Deferred: the PR's `pages` job going green and the deploy itself (needs GitHub Pages enabled — owner action). | `## 2026-09-23 — Task 82: MkDocs Material docs site built and wired to deploy` |
| 2026-09-23 | 81 | Adds `docs/plan/outreach/` — internal drafts only, nothing submitted/posted; owner posts by hand. `awesome-mcp-servers.md` (ready, Glama badge, rated A, `check-glama.yml` rule quoted), `awesome-java.md` (ready, live PR-template checklist filled, entry = tagline T), `awesome-spring.md` (hold — that list's MCP-servers subsection is Spring-AI-built servers only; Data Prism has no Spring AI dependency), `awesome-llm-security.md` (hold — scope; LLM Guard noted as partial precedent), `launch-post.md` (angle "Redaction breaks LLM investigations; consistent pseudonyms don't"; every claim linked; Limits section: not anonymisation, audit does not prove tail-truncation/whole-boot-deletion/metric leakage, fixed identifier-shape scanning not general PII detection, prompt injection flagged not defended, not-yet-built items; Show HN/r/java titles pairing "Data Prism" with "MCP privacy layer"; pre-post checklist incl. confirming `main`'s README no longer says the audit sink is "Not built"), and a `README.md` status table. Five attempts: attempt 1 (rejected) missed the Glama badge, had a "one file per writer" audit error, falsely claimed two `awesome-spring-ai` entries don't use Spring AI, gave titles without "MCP privacy layer", and omitted a free-text limit; attempts 2-5 tightened one launch-post sentence — "fails closed unconditionally" (only the default), an unsupported link for the relaxed setting, the DENY audit event's actual fields (no code/path/classification — those are the caller's error only), and "per field" (unclassified is one setting per profile, not per field). Tracing that sentence found a real shipped-doc defect, filed as task 84: `docs/configuration.md:73` claims two startup refusals that no code enforces. PASS + APPROVE on attempt 5, diff confirmed limited to the six files under `docs/plan/outreach/`, merged `--no-ff` onto the local `discoverability` branch (not `main`). Four further wording gaps left as unscheduled follow-ups in `docs/plan/PLAN.md` "Found on task 81", including whether `PASS_THROUGH_UNSAFE` is reachable today via classpath shadowing of the default profile file. | `## 2026-09-23 — Task 81: outreach drafts for the owner to post by hand` |
| 2026-09-23 | 84 | Corrects `docs/configuration.md:73`'s `dataprism.privacy` row, which claimed two startup refusals no code enforces ("a production profile that relaxes fail-closed behaviour", "a profile that lacks a rule required by exposed models") — found tracing task 81's launch-post claim back to code. Now states: no `dataprism.*` property loads a custom profile file, so only the bundled `privacy-profiles-default.yaml` loads, whose `DEFAULT` and `STRICT` profiles both set `unclassified: FAIL_REQUEST`; adds the real refusal, `FORBIDDEN_PRIVACY_OVERRIDE`, for an application `PrivacyPolicyResolver` bean. Re-verified fresh: the only profile-related startup codes are `UNKNOWN_PRIVACY_PROFILE`, `FORBIDDEN_PRIVACY_OVERRIDE`, `MISSING_PRIVACY_PROFILE`; `releasesUnclassifiedData()` has no callers; real Spring contexts started under both `STRICT` and `DEFAULT`. PASS + APPROVE on attempt 1, diff limited to the one table row, merged `--no-ff` onto the local `discoverability` branch (not `main`), then a close-out commit. Two further gaps left as unscheduled follow-ups in `docs/plan/PLAN.md` "Found on task 84": the scope-lifetime clause understates `protectedDeployment()`'s reach beyond "production", and stdio fixture-development with an unset `profile` likely NPEs instead of returning a clean refusal code. | `## 2026-09-23 — Task 84: the \`dataprism.privacy\` row's two false startup-refusal claims corrected` |
| 2026-09-23 | 77 | Adds `docs/faq.md` (seven H2 questions, answered first-sentence-first: output not anonymous per Art. 4(5); how pseudonyms are made and scoped; fixed identifier-shape scanning, not general PII/name detection; YAML path vs Java; audit trail proves/does-not-prove per `docs/audit.md`; `InstructionContentHeuristic` flags prompt injection but is deliberately not a defence; not production-ready, pre-1.0 per `SECURITY.md`) and `docs/comparison.md` (Microsoft Presidio, LLM Guard, NeMo Guardrails, Docker MCP Gateway — "different layers" table, "use X instead when" per tool, "where Data Prism does not fit"; maintainer-authored, dated 2026-09-23; every claim about another tool sourced to that tool's own docs with an access date; no quality/performance claims). Internal-doc links use absolute GitHub URLs so the docs site's strict build does not break. Two attempts: attempt 1 wrongly said the Docker MCP Gateway does not inspect tool responses (its own docs say `--block-secrets` scans them by default), invented a scanner history that never happened, mis-cited and understated Presidio's structured-data scope, left Data Prism's own claims unlinked, and linked an excluded internal doc; attempt 2 fixed all of these. PASS + APPROVE on attempt 2, diff confirmed limited to the two owned files, merged `--no-ff` onto the local `discoverability` branch (not `main`). Three wording gaps left as unscheduled follow-ups in `docs/plan/PLAN.md` "Found on task 77". | `## 2026-09-23 — Task 77: an FAQ and a fair, sourced comparison page` |
| 2026-09-23 | 76 | Applies canonical tagline T and description D (`docs/plan/specs/2026-09-23-discoverability.md`) across every surface search engines and AI assistants read: README opening (T, D, two-sentence "Who it's for"), root `pom.xml` `<description>`, `server.json` `.description` (96 chars, effective at next MCP registry publish), five new OCI labels on `docker/distribution/Dockerfile`, a fixture-only description label on `docker/server/Dockerfile`, and a new `CITATION.cff` (CFF 1.2.0, no version/date). Adds build/Maven-Central/licence badges and `<!-- site-intro:start/end -->` markers for task 82's docs site. Corrects the README Status paragraph, which wrongly said the hash-chained audit sink and verifier were "Not built" (`CHANGELOG.md` `[0.3.0]` shipped both), with every claim traced to CHANGELOG/audit.md/tools.md. Replaces `docs/extending.md`'s three `README.md:NNN` line citations with section names. No version changed anywhere. Two attempts: attempt 1 left a stale "19 Maven submodules" count (18, since `data-prism-audit` merged into core) and shifted two `docs/extending.md` line citations onto the wrong lines when the new Dockerfile labels grew the file; attempt 2 fixed both and moved to section-name citations. Verified: `mcp-publisher validate`, the three `publish-mcp.yml` name extractions agreeing, `docker buildx build --check` on both Dockerfiles, built-image labels via `docker inspect`, `mvn -B -q -N validate`, `cffconvert --validate`, badge URLs. PASS + APPROVE on attempt 2, diff confirmed limited to the seven owned files, merged `--no-ff` onto the local `discoverability` branch (not `main`). | `## 2026-09-23 — Task 76: one canonical identity across README, poms, registry and image metadata` |
| 2026-09-23 | 78 | Adds three use-case pages under `docs/use-cases/`: pseudonymise customer data for an LLM agent in Spring Boot (names the starter and the standalone server as the two deployment options), GDPR data minimisation for MCP tools (links Art. 4(5), 5(1)(c), 25 and 32 to EUR-Lex 32016R0679, quotes 5(1)(c) and Recital 26 verbatim, "this is not legal advice", lists the operator's own obligations, states pseudonymised data is still personal data), and consistent pseudonyms across systems (pseudonym collapse, consistency findings, scope isolation, linked to `docs/tools.md` by anchor). Each routes into existing docs rather than duplicating them; no new configuration or code snippets; every claim traces to a doc. Three attempts, all on paraphrase accuracy: attempt 1 overstated pseudonym-consistency scope, mis-attributed a Recital-26 statement to Art. 4(5), paraphrased Art. 5(1)(c) beyond its text, left two citations unlinked, and claimed `configuration.md` documents key rotation (it does not); attempt 2 fixed those but added an unsupported "reversible" reading of Art. 4(5); attempt 3 dropped it and quoted Recital 26 verbatim. PASS + APPROVE on attempt 3, diff confirmed limited to `docs/use-cases/**`, merged `--no-ff` onto the local `discoverability` branch (not `main`). | `## 2026-09-23 — Task 78: three use-case pages for the searches this audience runs` |
| 2026-09-23 | 80 | Adds `docs/plan/discoverability/` — `questions.md` (15 audience questions, none naming Data Prism), `runs/TEMPLATE.md` (4 assistants x 15 questions, citation rate = cited/60), `snapshot.sh` (bash+gh+jq, GET-only, fixed `AindriuB/data-prism` target overridable via `DATA_PRISM_REPO`, output dir from the script's own location, refuses to overwrite a same-day snapshot without `--force`, `mktemp`+`mv` under `set -euo pipefail` so no partial writes), `baseline.md` plus the committed `snapshots/2026-09-23.json` and raw copies (1 star, 0 forks, 7 issues; 318/9 views and 1358/346 clones over the v0.3.0-release window, mostly owner/CI; github.com the only referrer), and a `README.md` procedure (monthly assistant check, 14-day snapshot cadence, numbers to read by hand). Attempt 1 passed test and review but needed three `snapshot.sh` fixes before merge: it inferred the target repo from the working directory (so running it from elsewhere silently recorded the wrong repo's traffic), it silently clobbered a same-day snapshot (a live run overwrote the committed baseline), and its header overstated read-only/GET-only access without noting the traffic endpoints need push access. Attempt 2 fixed all three, reverified by running from a different repo entirely. PASS + APPROVE on attempt 2, diff confirmed limited to `docs/plan/discoverability/**`, merged `--no-ff` onto the local `discoverability` branch (not `main`). | `## 2026-09-23 — Task 80: discoverability gets a tracker-free measurement setup` |
| 2026-09-23 | 79 | Adds `docs/assets/social-card.png` (1280x640, tagline T, "Data Prism" and nothing else) plus `docs-site/social-card/{make_card.py,requirements.txt,README.md}`, which task 82 wires into the site's `og:image`/Twitter card meta and the owner uploads as the GitHub social preview. Pins Pillow 11.3.0 (not the task file's suggested 10.1.0, which has no `cp314` wheel) and uses Pillow's own built-in scalable default font instead of committing a font file, so regeneration in a fresh venv is byte-identical to the committed PNG, confirmed on both Python 3.14 and `python:3.12-slim`. PASS + APPROVE on attempt 1, diff confirmed limited to the two owned paths, merged `--no-ff` onto the local `discoverability` branch (not `main`). | `## 2026-09-23 — Task 79: the site gets a reproducible social card` |
| 2026-09-23 | 59 | Ends `docs/quickstart.md` with a "What next" pointer to `docs/protect-your-own-api.md` instead of dead-ending at `docker compose down`; replaces its four hand-scraped curl/`python3 -c` blocks with `examples/quickstart-demo/run.sh`'s real output for both customer 1001 and 1002; documents pull-by-default Compose with `QUICKSTART_IMAGE_TAG=0.3.0` pinning alongside the from-source override in both `docs/quickstart.md` and `README.md`; adds `dataprism.identity.resolver`, all ten audit/identity refusal codes, task 75's writer-id rules, and the one-level nested-catalogue grammar (with its request-time refusal codes correctly attributed to `data-prism-connectors-rest`) to `docs/configuration.md`; widens `run.sh` so its raw-value leak check is proven load-bearing for both demo subjects instead of vacuous for one. Four attempts: attempts 1-2 rejected for describing the demo instead of running it (a false pseudonym-determinism claim, a leak check that hardcoded subject 1001's values, an audit-code claim disproved only by standing up a real Spring context in stdio fixture-development mode). Attempt 3 PASS + APPROVE in full; owner then asked to cut one disproportionate paragraph. Attempt 4 (the cut plus one sentence reworded to stop overclaiming) is reviewer APPROVE with no defects; its tester run was still in progress when the owner asked to record the task closed, and chose to record without waiting. Merged `--no-ff` onto `v0.3.0/quickstart-exit-ramp` (local, cut from `main` at the PR #96 merge, not yet pushed or PR'd). Post-merge full-reactor verify: 646 tests, 0 failures. | `## 2026-09-23 — Task 59: the quickstart gets an exit ramp, and the reference docs catch up to v0.3.0` |
| 2026-09-23 | 70 | Cuts version 0.3.0 across the reactor — poms, `server.json` (plus the missing `DATAPRISM_AUDIT_FILE_PATH` variable), both `serverInfo` literals, the distribution Dockerfile, `publish-image.yml`'s dispatch default, PackagingIT/SmokeIT jar names, README/doc literals — and writes `CHANGELOG.md`'s `[0.3.0]` entry from the merged diffs of tasks 60-69, naming every breaking change with the consumer action it requires (71's 40-bit/24-byte-MAC pseudonym discriminator, 73's `AUDIT_SINK_BEAN_REQUIRED`, 75's `instanceId` shape and `INVALID_AUDIT_WRITER`, 69's `UNRESOLVED_SOURCE_ADAPTER`→`UNREVIEWED_SOURCE_ADAPTER`). Verified: fresh-repo build green, `-Prelease` reaches GPG signing, packaged server reports `serverInfo` 0.3.0 over real MCP, all five images build, `server.json` validates against the live schema. Four attempts, all on CHANGELOG accuracy and test-count bookkeeping, not the version bump; lessons on writing a CHANGELOG from verified sources rather than plan text, never citing commit SHAs on a branch that will be rebased, and why summing surefire `.txt` files undercounts `@Nested` JUnit 5 classes (632 vs the true 646). PASS + APPROVE on attempt 4, merged onto `v0.3.0/audit-trail-and-nested-json` — **not yet onto `main`**; the integration branch's own merge to `main` is a separate, still-pending owner step. | `## 2026-09-23 — Task 70: version 0.3.0 cut across the reactor, onto the integration branch` |
| 2026-09-23 | 62 | Corrects `docs/architecture.md`'s flat-by-design and boundary-7 claims to what task 60 shipped (one-level nested catalogues, `AuditFilePiiScanTest` named); ships new `docs/audit.md` documenting the hash chain, `FileAuditSink`, the offline verifier CLI, all five exit codes and their precedence, the per-boot `<writer-id>/<uuid>` `instanceId`, and exactly what is/is not detected (edits anywhere including the last record are caught; tail deletion and whole-boot deletion are not; Slf4j log input reads as benign interrupted writes, exit 4, never point the verifier at logs); refreshes `docs/protect-your-own-api.md`'s nested walkthrough with task 71's pseudonyms and the `NESTED_LEAF_NOT_SCALAR` code; repositions `docs/extending.md`; adds `examples/json-sources/customer-api-nested.yaml` and `NestedCatalogueWalkthrough.java`, reproducible from an empty local Maven repo. Nine attempts, all but one failing on review only — nearly every round found one new factual error the last missed, several from a reviewer's own source-reading rather than tracing to the enforcing code (attempt 1's reviewer claimed exit 2 on log input; the tester ran it and got exit 4). Converged once testers compared quoted output BY TEXT against real runs and reviewers traced every claim to code. Task 75 (per-boot chain identity) was spun out of this task's attempt-5 review. PASS + APPROVE on attempt 9, merged onto `v0.3.0/audit-trail-and-nested-json`. | `## 2026-09-23 — Task 62: nested catalogues and the durable audit chain are documented` |
| 2026-09-23 | 75 | Found on task 62's attempt-5 review: a hash-chained server restarted with the same `dataprism.audit.writer-id` raised a false CHAIN BREAK, since every boot restarted at GENESIS/sequence 1 under one config-fixed `instanceId`. `AuditRecorder` now derives `instanceId` as `<writer-id>/<per-boot random UUID>`, so a restart reads as a new writer at GENESIS instead of a chain break; a blank or `/`-containing writer-id refuses (`AuditRecorder` `IllegalArgumentException`; startup `MISSING_AUDIT_WRITER`/new `INVALID_AUDIT_WRITER`). `AuditChainVerifier` untouched; restart, edit/delete and non-GENESIS detection all verified unchanged against the real packaged server. New owner-accepted blind spot: deleting an entire boot's records is as undetectable as tail truncation. Three attempts: attempt 1 made `PiiLogScanTest` flaky (a random UUID could contain a banned fixture value); attempt 2 fixed the flake but both new PII-scan exemptions skipped the whole field on match, so a banned value inside the config-supplied writer-id was never scanned — a fail-open in a leak scan; attempt 3 exempts only the anchored `/<uuid>` suffix. PASS + APPROVE on attempt 3, merged onto `v0.3.0/audit-trail-and-nested-json`. Unblocks task 70 and task 62 (which must now document the new instanceId shape and the whole-boot-deletion blind spot). | `## 2026-09-23 — Task 75: each process boot gets its own audit chain identity` |
| 2026-09-23 | 69 | Restores the reviewed-adapter allow-list task 54's subset relaxation had silently dropped: `DataPrismContractValidator` refuses (`UNREVIEWED_SOURCE_ADAPTER`) any `DataSourceAdapter` bean named by neither `dataprism.sources` nor the JSON catalogue's own published source names, via a plain `Set<String>` bean (`dataPrismConfiguredJsonSourceNames`) consumed with `ObjectProvider`+`@Qualifier`. Attempt 1 (rejected) instead wired a connector-owned record type (`ConfiguredJsonSourceNames`) via `ObjectProvider` into unconditional `@Bean` signatures, backed by a new optional Maven dependency from autoconfigure to connectors-rest — crashed the base standalone server (`TypeNotPresentException`) because Spring resolves `@Bean` parameter types via `Class.forName` before `ObjectProvider.getIfAvailable()` runs, invisible to `mvn test` and caught only by `mvn verify`. Squashed both commits into one merge commit since attempt 1's message describes deleted code. Bean-name collision safety (named-absent resolves to null, not a fallback bean) verified empirically against real Spring contexts. PASS + APPROVE on attempt 2, merged onto `v0.3.0/audit-trail-and-nested-json`. Follow-up filed: the three-literal bean-name contract has no shared constant and is checked only by `mvn verify`. | Task 69: the reviewed-adapter allow-list is restored, via a `Set<String>` bean instead of a connector-owned type |
| 2026-09-23 | 55 | Publishes the four Compose quickstart images (server-with-extension, fixtures, issuer, certs-init) as multi-arch GHCR manifests on a `v*` tag, alongside the existing distribution image, under the same registry-touching guard; `compose.yaml` now pulls them by default, with the from-source path moved to `compose.build.yaml`. Fix-pass commit closed a release-day landmine — the from-source Dockerfiles pinned `ARG VERSION=0.2.0`, which task 70's reactor cut to 0.3.0 would have broken at `COPY` on a missing jar — by locating each module's one repackaged jar by `find` instead, version-independent, verified by an actual `mvn versions:set -DnewVersion=0.3.0` rebuild. Close-out review removed six dangling `--build-arg VERSION` call sites left over from that fix and corrected an inaccurate Dockerfile comment that credited a no-op `find` predicate with excluding the repackage plugin's `.original` sibling. Verified PASS/APPROVE twice; not squashed, three distinct commits. Not exercised: a full `docker compose up` end-to-end run, and the GHCR publish path itself (argued from conditions, same allowance as task 40). | Task 55: the quickstart images publish, and Compose pulls them by default |
| 2026-09-23 | 67 | Wires `dataprism.audit.sink=hash-chained` to `FileAuditSink`, bound to the required `dataprism.audit.file-path`; missing path refuses at config validation (`MISSING_AUDIT_FILE_PATH`), an unopenable path refuses at startup (`AUDIT_SINK_FILE_UNUSABLE`, proven by mutation against a silent fallback to `Slf4jAuditSink`), `slf4j` still resolves only `Slf4jAuditSink`. Because task 72 merged first, this is the first durable hash-chained audit file ever written, already under the nineteen-field hash — nothing to migrate. Attempt 1 was rejected on review not for broken wiring but because two of its claims (a leak the response mapper no longer has, a hand-rolled chain replay task 66's now-merged verifier made unnecessary) went false while it sat verified against a base that had moved under it; attempt 2 corrected both, added the missing `AUDIT_SINK_FILE_UNUSABLE` test, and preserved the sink's exception cause server-side. Squashed both commits into one before merging so history does not carry the corrected commit's false claims. Widens `docs/conventions.md`'s catch-and-log exception paragraph to a stated rule instead of a two-site list, now covering a third site. Tamper-evident against an outside forger only, not the operator — `AuditEventHash` stays unkeyed SHA-256, unchanged and unclaimed otherwise. | Task 67: `dataprism.audit.sink=hash-chained` wired to `FileAuditSink` |
| 2026-09-23 | 71 | Widens `HmacSyntheticGenerator`'s discriminator from 20 bits (four Crockford characters) to 40 bits (eight, `d[i] & 0x1F` over eight digest bytes), matching `HmacValueTokenSource.token`; `ADDRESS`, previously untagged (400-subject 50% collision floor), now carries the tag every other namespace does. `PseudonymisationVersion`'s compact constructor gained a digest-length guard (`InvalidAlgorithmException`, codes `pseudonymisation.algorithm-digest-too-short`/`-unavailable`), closing an `ArrayIndexOutOfBoundsException` path for `HmacMD5`/`HmacSHA1`. Both golden-vector files regenerated under the dated `PLAN.md` exception; `version` stays `v1`. Both defects found by the repository owner in review, not by planning. No collision-resistance claim made or implied. Commit-body colliding-pair literals are illustrative of a reproduction, not reproducible constants — only the `ADDRESS` pair matched exactly across independent mutations. Flags (does not fix) a documented, non-firing default-`SSLContext` race between `AuditSinkFailureAbortsResponseTest` and `McpHttpEndToEndTest`/`ConfiguredJsonNestedHttpTest` — five runs, all green. PASS + APPROVE, merged onto `v0.3.0/audit-trail-and-nested-json` | `## 2026-09-23 — Task 71: pseudonym discriminator widens from 20 to 40 bits, and \`ADDRESS\` gains one` |
| 2026-09-23 | 74 | Stops a throwing `AuditSink`'s raw exception reaching the MCP client: `GetEntityContextTool`/`CompareEntitySourcesTool` catch only the `audit.record(...)` call and rethrow `AuditUnavailableException` (code `AUDIT_UNAVAILABLE`), cause kept server-side via `addSuppressed` rather than as a `cause` — proven by mutation that a plain cause lets the MCP SDK's `aggregateExceptionMessages` disclose a real `FileAuditSink` path through `McpError`'s `data` field. Abort-on-audit-failure guarantee (task 63) re-verified unchanged. Closes the hard precondition on tasks 62/59 and unblocks task 67. `LOG.error(msg, cause)` in both tools recorded as a deliberate, reviewed exception to `docs/conventions.md`'s catch-logging rule. PASS + APPROVE, merged onto `v0.3.0/audit-trail-and-nested-json` | `## 2026-09-23 — Task 74: a sink's exception no longer reaches the MCP client verbatim` |
| 2026-09-23 | 73 | Raises a new code, `AUDIT_SINK_BEAN_REQUIRED` (naming `dataprism.audit.sink=approved-sink` and the required bean type), from `DataPrismContractValidator` when that specific configured value has no `AuditSink` bean; `MISSING_AUDIT_SINK` retained for the absent-or-blank-property case and for any other accepted value reaching the same branch (`hash-chained`, until task 67 gives it a bean) — a value-based split verified against `DataPrismAutoConfiguration` rather than assumed. All eight pinning fixtures unchanged in intent. Closes the bookkeeping condition task 67's reviewer required before recording 67 done. Leaves `docs/configuration.md` with no entry for the new code, owed to tasks 59/62. PASS + APPROVE, merged onto `v0.3.0/audit-trail-and-nested-json` | `## 2026-09-23 — Task 73: \`AUDIT_SINK_BEAN_REQUIRED\` names the cause instead of the symptom` |
| 2026-09-23 | 72 | Widens `AuditEventHash.compute`'s join from seventeen to nineteen fields, folding in `timestamp` (`Instant.toString()`) and `sourceSystems` (sorted like `rejectedArguments`); `AuditRecorder` reads the clock once and reuses that instant for both the hash and the event. Verifier/CLI limitation text and javadoc corrected from "seventeen" to "nineteen"; pinned test literal recomputed independently. Closes the lazy-edit gap task 66's own limitation text named (a backdated record no longer verifies clean) but does not touch the unkeyed-SHA-256/operator-recomputable gap, which stays open by owner decision. Landed before task 67 deliberately, since no durable hash-chained file exists yet and widening after 67 would invalidate real chains. Single commit `43badb8`, fast-forward merge, PASS + APPROVE | `## 2026-09-23 — Task 72: \`timestamp\` and \`sourceSystems\` brought inside the audit hash` |
| 2026-09-23 | 66 | Ships the offline audit-chain verifier CLI: replays each writer's per-`instanceId` hash chain, reuses `AuditEventHash`, five exit codes (0 intact, 1 unreadable, 2 break, 3 possibly-in-flight tail, 4 structural anomaly), distinguishes four ordinary failure modes from tampering (torn trailing record, mid-file restart fragment, duplicate sequence, new writer at GENESIS mid-file), prints its limitation on every path including `--help`. Four attempts, each rejection the same defect relocated: attempt 1 let head deletion read as intact and overclaimed detection of timestamp/sourceSystems edits; attempt 2 fixed both then added a `precededByAnomaly` exemption that laundered three real attacks into "intact"/"not tampering"; attempt 3 removed the exemption but exit 4's `--help` text still asserted "non-tampering"; attempt 4 approved. Severity split (non-GENESIS start alone = break, preceded by an anomaly = downgraded to structural) ruled intentional, not a bug. Merged onto `v0.3.0/audit-trail-and-nested-json` | `## 2026-09-23 — Task 66: the offline audit-chain verifier CLI, after four attempts each relocating the same defect` |
| 2026-09-22 | 61, 65 | v0.3.0 wave 2: task 61 proves the nested-catalogue path through the real MCP HTTP/SSE transport (real SSE parsing, real mTLS upstream, all three task-60 refusal codes proven with raw fixture values absent, no pinned pseudonym so task 71 cannot break it); task 65 ships the file sink's PII scan after two attempts (attempt 1's `BANNED_VALUES` was consumed only by the main test, so an empty derivation would have passed clean — the eighth cannot-fail assertion counted; attempt 2 added the non-empty-plus-known-values guard and reflection over `AuditEvent`'s record components). Both merged, PASS + APPROVE. 66 (audit chain verifier) held at attempt 1 REQUEST CHANGES — head deletion reads as intact, limitation text overclaims. 67 (wire hash-chained) held PASS/REQUEST CHANGES for bookkeeping (`approved-sink` accepted with no bean). A plain `mvn verify` without `clean` hit a stale incremental-compile artefact, pre-existing | `## 2026-09-22 — v0.3.0 wave 2 (tasks 61, 65): nested JSON proven through the real MCP transport, and the file sink's PII scan closed after an empty-derivation hole` |
| 2026-09-22 | 60 | Adds one level of named nested sub-catalogues (`nested: <name>`) to the configuration-driven JSON REST connector, so a flat-but-for-one-level JSON API can be protected in YAML with no Java; `data-prism-core` unchanged. Descend key is a bounded pool of 16 pre-declared marker classes, ordinals a pure function of the sorted catalogue-name set. Three attempts: attempt 1 shipped a fail-open (scalar at a `nested:` field emitted raw) that also exposed a gap in the task file's own acceptance criteria, plus a rejected runtime-hidden-class mechanism; attempt 2 closed both but its slot ordinals were non-deterministic across JVMs (`Map.copyOf` iteration order) and a `nonSensitive:` reason could collide with the reserved nested-pointer prefix; attempt 3 (PASS + APPROVE) fixed both, proven by cross-process determinism tests. Closes v0.3.0 wave 1 (60, 63, 64, 68 all merged); wave 2 (61, 65, 66, 67) now unblocked. Post-merge full reactor: 1018 tests, 0 failures | `## 2026-09-22 — Task 60: one level of named nested JSON catalogues, after three attempts and a task-file amendment` |
| 2026-09-22 | 63, 64, 68 | v0.3.0 wave 1: fixed `AuditRecorder`'s hash-chain write ordering so a throwing sink cannot advance the chain past an unwritten record (63); shipped `FileAuditSink`, a durable append-only sink, after three attempts closing a null-component NPE and a mid-file concatenation from a poisoned short write (64); closed the nested/`@Import`ed bean-classification escape hatch task 53 left and widened the sweep to walk it (68). Records nine follow-up items and the four ordinary failure modes that resemble tampering, for task 66's verifier. Task 60 (nested JSON catalogues) is still in rework at attempt 3 and does not merge here; task 55 remains held | `## 2026-09-22 — v0.3.0 wave 1 (tasks 63, 64, 68): the audit chain's write ordering fixed, a durable file sink shipped after three attempts, and a bean-classification escape hatch closed` |
| 2026-09-22 | 58 | Ships `docs/protect-your-own-api.md`, a YAML-only walkthrough taking a reader with a flat JSON REST API to a pseudonymised MCP response with no Java, using task 53's `dataprism.identity.resolver: pass-through` and task 54's collapsed base-url check. Publishes the former test-only catalogue as `examples/json-sources/customer-api.yaml`; repositions `docs/extending.md` as the escape hatch for what a flat catalogue cannot express. Six attempts: read-through-approved-but-broken (1), a private-key-into-the-repo rule 3 breach (2), a session-scoped shell state bug reproduced as a `FileNotFoundException` (3), a pre-set-variable footgun that could delete a reader's real keystores (4), a half-finished tidy that left the same failure mode at a variable the inlining missed (5), approved (6). No independent tester ran attempt 6; attempt 5 passed full independent multi-shell verification, and 6's diff was confirmed narrow by review. Merged `bfabcb1`. No task file remains under `docs/plan/tasks/` | `## 2026-09-22 — Task 58: a walkthrough for a stranger's own API, and six attempts to make it both work and not write key material into the repo` |
| 2026-09-21 | 53, 54, 56, 57 | Made the configuration-driven REST connector actually no-code: opt-in `dataprism.identity.resolver: pass-through` (53), collapsed duplicate `dataprism.sources` base-url check via `containsAll` (54), extracted a one-command demo whose first attempt failed against the real MCP transport because it was only tested against a JSON stub (56), and a Spring `FailureAnalyzer` rendering every configuration refusal as an operator block with no stack frame (57). Task 55 (publish quickstart images) verified PASS/APPROVE but held by owner decision — not merged | `## 2026-09-21 — Wave 1 (tasks 53, 54, 56, 57): the no-code path made real, and the demo's transport bug that a stub had hidden` |
| 2026-09-17 | 52 | Reconciled shipped docs against 0.2.0. Most consequential: `README.md`'s "Until Task 20 delivers…" JSON-REST claim was stale since task 20 shipped it — corrected from code (`data-prism-connectors-rest` is normally published, self-registering, needs no Java; `ConfiguredJsonFieldMetadataResolver.descendable()` returns `false` unconditionally, so flat JSON only). Planned against four stale "only one tool" claims; count grew twice under examination to six, including one the executing scribe found independently in `docs/architecture.md`'s Deployment paragraph. All six corrected from a real `tools/list` response captured over a FIFO stdio session, not inferred from registration code. `ArchitectureTest` ownership re-pointed to `data-prism-architecture`; `docs/extending.md`/`docs/tools.md` linked in. A false causal claim (`tools/list` implied capability-filtered) caught and fixed at review, replaced with the true grant-before-call fact: the Compose quickstart's `investigator` role can list `compare_entity_sources` but not call it. Task file's own cited line numbers were stale in three places, including a section-header/claim mismatch at `docs/quickstart.md:81` vs `:113`. Merged #84. No task file remains under `docs/plan/tasks/` | `## 2026-09-17 — Task 52: the shipped docs reconciled — module rename, six stale tool/mode claims, and a false causal claim caught at review` |
| 2026-09-17 | 49, 50, 51 | `data-prism-example` renamed to `data-prism-integration-tests` (11 test classes, no duplicate elsewhere, including the sole enforcement of privacy rule 7) — a name that invited deletion of the thing guarding a privacy rule; nothing removed. `ArchitectureCoverageTest`'s dynamic module list (a class whose own javadoc records a prior silent narrowing) was proven non-narrowed by mutation and instrumented printing, not just a green run. Six `data-prism-example` config-value/test strings left alone deliberately (audit output, not a module name). Ships the first two consumer guides, `docs/extending.md` and `docs/tools.md`, both built from real captures (a real annotation-processor error, a real `UNRESOLVED_SOURCE_ADAPTER` refusal, a real `TOOL_NOT_PERMITTED` refusal, a real pseudonym-collapse case) rather than transcribed. Task 50's pom snippet was "verified" by an external build that silently added `maven.compiler.release=21`, shipping a `records are not supported in -source 8` defect until the guide's fences were extracted programmatically instead of retyped. Sequential-merge race hit twice (49 then 51 then 50, each a clean rebase). Merged #80, #82, #81. Leaves the stale `README.md:156-158` JSON-REST claim and the external demo's non-public status for task 52 | `## 2026-09-17 — Tasks 49, 50, 51: \`data-prism-example\` renamed to \`data-prism-integration-tests\`, and the first two consumer guides ship` |
| 2026-09-17 | 48 | 0.2.0 release complete: task 48 fixed `server.json`'s OCI package block (400: `registryBaseUrl` forbidden, found by reading the registry's server-side `ValidateOCI` source, which also revealed `version` forbidden — never named by the error) and extended the identifier-tag guard. GitHub Release, Maven Central, GHCR multi-arch image, and the MCP registry entry (`io.github.AindriuB/data-prism@0.2.0`, `active`) are all live — the registry publish succeeded on the third attempt (403 casing fixed by task 44, 400 OCI shape fixed by task 48), a green `mcp-publisher validate` predicted neither failure. `v0.2.0` was force-moved from the rejected commit to PR #78's merge commit; the only changed files are non-artifact-affecting (`server.json`, `publish-mcp.yml`, `CHANGELOG.md`), unlike the 0.1.1 case where a tag move was rejected. No task file remains under `docs/plan/tasks/` | `## 2026-09-17 — 0.2.0 release complete: GitHub Release, Maven Central, GHCR multi-arch, and the first successful MCP registry publish` |
| 2026-09-17 | 47 | Closes the two same-shaped holes task 46 left: derivation now recurses through `Record` components and `Collection` elements to arbitrary depth (depth-capped, proven to terminate against a self-referential structure) instead of stopping one level deep and banning a nested component's `toString()`; `findLeaked`'s bounds changed from `\b` to `(?<!\w)`/`(?!\w)`, closing the case where a banned value ending in punctuation (both order notes) could never match at end-of-line or before a space. `OrderDto` gains one nested `@SensitiveObject` component, `DeliveryDto`, so the recursion is falsifiable against a real fixture. Both holes proven by mutation, watching the pre-fix code stay GREEN with the leak still live — not just the fix going green. The hex-collision defence the `\b` bound existed for is pinned by a test shown RED under `contains` and GREEN under both bound versions, before the bound changed. Banned set is classification-blind by design: `@NonSensitive` authorises a value into the response, not the log. `docs/agents/stdio.md:102`'s captured entity tree fixed to include the new `delivery` block, re-captured by actually driving the stdio fixture server. Merged #76 (landed on task 45 first), 492 tests | `## 2026-09-17 — Task 47: the PII scan's reflection-depth and word-boundary holes closed` |
| 2026-09-17 | 45 | Cuts version 0.2.0 — 19 module poms plus root, `server.json` (both fields), the two `serverInfo` literals, four Dockerfiles, `README.md`, three PackagingIT/SmokeIT tests — so `compare_entity_sources` (42/43) and the corrected registry namespace (44) can ship. `CHANGELOG.md`'s 0.2.0 entry checked line by line against tasks 42-44's diffs before being written. Found, with `rg --hidden`, that every prior "no old version remains" sweep in this repository had been running blind to `.github/`: two stale `0.1.1` literals survived in `publish-image.yml`'s `workflow_dispatch` input (lines 53, 55), not a safety hole since the version guard fails closed, but would have wasted a release attempt. Coordinator authorized a scope extension beyond the task file's `Owns`/out-of-scope lines, limited to exactly those two literals; reviewer confirmed the rest of the workflow (guards, matrix, digest-push, manifest assembly) is byte-identical to `main`. Merged #75, 488 tests unchanged | `## 2026-09-17 — Task 45: version 0.2.0 cut, and a sweep that had been running blind to \`.github/\`` |
| 2026-09-17 | 46 | Derives `PiiLogScanTest`'s banned-value set from `Stub{Customer,Account,Order}Adapter`'s own fixture records instead of a hand-maintained literal list, yielding 16 values including six the old list omitted (`Pat Murphy`, `P. Murphy`, `ACC-1`, `ORD-9`, `4200.55`, `18.00`). Proven by mutation: a raw record logged from `SourceFanOut`, a production path, reddened the scan naming exactly those six, which the old list would have missed. `CustomerDto.status`'s exclusion was challenged in review and confirmed pre-emptive, not a silencing, by testing it included in an isolated clone. Implementer's own test count was low by 25 (462/463 vs real 487/488) from a restricted `-pl` reactor run — the second such counting error in this repo. Leaves two same-shaped holes as task 47: one-level-deep reflection, and a `\b`-bound leak matcher that misses values ending in punctuation. Merged #73, 488 tests | `## 2026-09-17 — Task 46: PiiLogScanTest's banned values derived from the stub fixtures` |
| 2026-09-17 | 43 | Wires `compare_entity_sources` into the example on the real `DataPrismAssembly.standard()` (real adapters, real `JsonTreeScrubbingEngine`, real audit) and proves no raw fixture value reaches its output. Settles the assumption task 42's stub-only tests could not: the real scrubbed tree is keyed by `FieldMetadata.fieldName()`, not the namespace constant — proven by mutating the real engine, not arguable from a stub. Pins the shipped role in all three places it is granted, not just two. Opens task 46: `PiiLogScanTest`'s hand-maintained banned-value list omits fixture spellings the merged tree already carries, a weakened control, not a breach. Third instance this cycle of a downstream agent correcting a coordinator's factual premise. Merged #71, 487 tests | `## 2026-09-17 — Task 43: compare_entity_sources wired into the example, and the identity assumption task 42 could not prove is now proven` |
| 2026-09-17 | 42, 44 | Ships `compare_entity_sources`, the second MCP tool: per-field `identity` plus agreement/disagreement/`MISSING_IN_SOME_SOURCES` findings over the scrubbed `ContextResponse`; corrects the MCP registry namespace to `io.github.AindriuB/data-prism` with a cross-file drift guard. Defect found in review: `identity` looked up fields by namespace name against a tree keyed by serialised field name, so it was silently empty for every realistic model — all three of the task's own `identity` tests shared that assumption instead of testing it, none could have caught it. `ContextRequest`/`ContextResponse` record components verified with `javap` against the published 0.1.0/0.1.1 jar to not break linkage. Agreement computed pre-scrub in `NamespaceCorrelationService`, since scrubbing collapses distinct values to the same pseudonym. Registry fix does not clear the 403 alone — the image label is baked in at build time. 483 tests (466 baseline, +17), merged #69 and #68 | `## 2026-09-17 — Tasks 42 and 44: the second MCP tool, and the registry namespace corrected` |
| 2026-09-17 | 41 | Cuts version 0.1.1 across the reactor, `server.json`, the MCP `serverInfo` handshake literals, all four Dockerfiles and `publish-image.yml`'s comment — because the `v0.1.0` tag predates the multi-architecture publish pipeline and `main` has diverged from the tag (CI/docs/issuer-pom only), not because of the nimbus-jose-jwt claim the task was planned on. That claim was false: PR #55 only pins a version in the quickstart issuer's pom, which no shipped image builds. Reviewer caught it and required a correction commit before approving — the second false planning premise caught at review, not implementation, this release cycle. Merged #66, carrying the previously-unpushed `plan: task 41` commit along with it. 466 tests, unchanged | `## 2026-09-17 — Task 41: cut version 0.1.1, and a planning false premise caught at review` |
| 2026-09-16 | 40 | Rebuilds `publish-image.yml` as a native per-architecture matrix (`ubuntu-latest`/`ubuntu-24.04-arm`, each verifying its own no-config refusal natively before pushing by digest) with a final job assembling a `linux/amd64` + `linux/arm64` manifest list — `docker save`/`load` cannot carry one, so this replaced the pipeline shape, not a flag. GitHub Actions run 35162338759 proved the arm64 runner and native verification both work and every registry-touching step is skipped off a tag. Local execution (not inspection) caught that the default buildx driver rejects `push-by-digest=true`; fixed with an explicit `docker-container` builder. Honest gaps recorded: the pushed image is a second `buildx build`, not the verified `--load`ed one (same job, warm cache); the tag-push no-write path and the digest-push/manifest-assembly steps themselves are argued from the conditions, unexercised by any real run yet | `## 2026-09-16 — Task 40: publish the server image for linux/amd64 and linux/arm64` |
| 2026-09-16 | 38 | Release plan closes. MCP registry entry (`server.json`, `mcp-name` marker, `publish-mcp.yml`) for `io.github.aindriub/data-prism`, gated like the other two publish workflows. Reviewer caught a listing defect that would have shipped a broken public onboarding contract: `DATAPRISM_SECURITY_POLICY_ROLES_INVESTIGATOR` does not bind under Spring Boot's relaxed map-key enumeration for a hyphenated prefix; corrected to `DATAPRISM_SECURITYPOLICY_ROLES_INVESTIGATOR`, confirmed against the real `Binder`. `publish-mcp.yml` refuses unless the image tag is already pullable and unless `server.json`'s version matches the packages entry. Nothing published yet, confirmed by a live registry search returning zero results | `## 2026-09-16 — Task 38: the MCP registry entry, and the release plan is complete` |
| 2026-09-16 | 36, 37, 39 | Release wave closes. Task 39 closes the default-`mode=HTTP` non-web fail-open reviewer 35 found (new `MCP_TRANSPORT_UNAVAILABLE`, one of four codes now meaning "no usable MCP transport"), breaking and fixing three modules' tests that depended on the bug; task 36 wires Maven Central publishing for 12 modules + root aggregator, all 49 signatures individually gpg-verified; task 37 ships the fixture-free distributable server image behind gated, unpublished workflows, closing three rounds of reviewer CHANGES each finding a sibling instance of the fix. Nothing published yet — owner actions (environment reviewers, secret scoping, MCP namespace claim) still outstanding. Serialized post-merge full-reactor re-run: 466 tests, 0 failures | `## 2026-09-16 — Tasks 36, 37, 39: Maven Central publishing, distributable server image, default-mode transport fail-open closed` |
| 2026-09-16 | 33, 34, 35 | Release wave opens: 0.1.0 cut across the reactor with a tag-triggered GitHub Release workflow that cannot publish to Central or a registry on its own (33); `CHANGELOG.md`/`SECURITY.md`/`dependabot.yml`/`CONTRIBUTING.md` security section, with a reviewer-caught personal-email leak in `SECURITY.md` fixed before merge (34); `dataprism.transport.mode=stdio` now refuses unconditionally in the shared Spring auto-configuration rather than starting with no MCP transport, `docs/configuration.md` corrected, `fixture-development` now dead-in-effect everywhere in the Spring surface (35). Found in review: a second fail-open at the default `mode=HTTP` for non-web starter apps, opened as task 39. Serialized post-merge full-reactor re-run: 462 tests, 0 failures — records the correct counting method after one tester's run double-counted surefire+failsafe as 920 | `## 2026-09-16 — Tasks 33, 34, 35: 0.1.0 cut, release hygiene files, stdio refuses instead of serving nothing` |
| 2026-09-15 | 31, 32 | Simplification wave 2: optional `dataprism.privacy.descriptor-file` property wires the previously-dead `core/descriptor` package behind `DescriptorFieldMetadataResolver`, four fail-closed refusal codes, no fallback; `data-prism-audit` deleted and its four classes moved into `data-prism-core` unchanged (package, hash chain, tests), reactor now 19 modules. Serialized post-merge full-reactor re-run: 460 tests, 0 failures. Records that concurrent Maven builds against this repo's shared local repository are unreliable (five spurious failures across two waves) and testers must serialize | `## 2026-09-15 — Simplification wave 2 (tasks 31-32): descriptor resolver wired, data-prism-audit merged into core` |
| 2026-09-15 | 26, 27, 28, 29, 30 | Simplification wave 1: shared `JwtDecoderSupport`/`JwtCallerContextExtractor` replacing duplicated OIDC/SSRF logic in server and example; `DefaultContextOrchestrator` reduced to its two used constructors with the fetch loop lifted out; duplicated YAML `enumValue` parsing extracted to a core `StrictYaml` helper; `JsonTreeScrubbingEngine`'s owner-object params bundled into an `OwnerScope` record; `DataPrismProperties` reformatted (no behaviour change). Owns-list-by-package missed a cross-package caller (`ServerSecurityBoundaryTest`), fixed by scribe-authorized amendment — future Owns lists should derive from callers, not packages. Serialized post-merge full-reactor re-run (not the parallel testers', three of which hit transient shared-repo classpath failures): 440 tests, 0 failures | `## 2026-09-15 — Simplification wave 1 (tasks 26-30): shared JWT decoder, orchestrator cleanup, StrictYaml helper, OwnerScope record, properties formatting` |
| 2026-09-15 | 19, 20 | Plan's last wave. Tested MCP agent connection guides verified against a real client (Claude Code CLI 2.1.271), with a smoke test that now asserts real pseudonymisation instead of grepping "Connected". `data-prism-connectors-rest` wired into a configuration-driven JSON REST mode (13 to 49 tests) with fail-closed classification parity to the Java-first path; three review rounds closed an HTTPS-gate reimplementation drift, a plaintext relaxation, a silent transport-disagreement, and two more cannot-fail assertions; one review finding was tested and graded down rather than accepted on authority. Post-merge reactor: 441 tests, 0 failures | `## 2026-09-15 — Tasks 19 and 20: tested agent connection guides, and a configuration-driven JSON REST mode` |
| 2026-09-14 | 18 | First runnable demonstration in the repository: `docker compose up` brings up the standalone server, synthetic fixture APIs (`data-prism-quickstart-fixtures`) and a local JWT issuer (`data-prism-quickstart-issuer`), proving a real MCP handshake returns pseudonymised data via a reviewed extension jar (`data-prism-quickstart-extension`). Architecture coverage now scans 17 modules, 201 classes. Post-merge reactor: 403 tests, 0 failures | `## 2026-09-14 — Task 18: a reproducible local quickstart` |
| 2026-09-14 | 25 | Shared Hazelcast read budget wired for the `embedded` topology; `dataprism.hazelcast.topology` made a required explicit choice (`MISSING_CLUSTER_TOPOLOGY`/`UNSUPPORTED_HAZELCAST_TOPOLOGY`/`MISSING_SHARED_BUDGET`), closing the fifth reviewed defect from tasks 13-17. Post-merge reactor: 400 tests, 0 failures | `## 2026-09-14 — Task 25: wire the shared read budget, make the topology an explicit choice` |
| 2026-09-14 | 21, 22, 23, 24 | Fixture-development refusal moved to the shared validator; privacy-critical beans made non-replaceable with a classified extension-point sweep; architecture rules regained the whole module graph via a new scanning module; packaged-server secret scan now reaches every nested jar. Post-merge reactor: 394 tests, 0 failures | `## 2026-09-14 — Tasks 21, 22, 23 and 24: close the reviewed defects in tasks 13-17` |
| 2026-09-14 | 17 | Executable fail-closed standalone server with JWT-authenticated configurable MCP path, reviewed extension loading, and fixture-free packaging | `## 2026-09-14 — Task 17: package the standalone Data Prism server` |
| 2026-09-14 | 16 | Dependency-only Spring Boot starter, shared `/mcp` lifecycle wiring, and an HTTPS-JWKS embedded example with fail-closed startup proofs | `## 2026-09-14 — Task 16: ship the Spring Boot starter and embedded protected-API example` |
| 2026-09-14 | 15 | Shared Spring Boot configuration binding and fail-closed startup validation, including operational HMAC secret-reference resolution | `## 2026-09-14 — Task 15: build the validated Spring Boot configuration core` |
| 2026-09-13 | 14 | Shared standalone-server/Spring-starter configuration contract, safe mode boundaries, and ADR for auto-configuration ownership | `## 2026-09-13 — Task 14: define the shared deployment configuration contract` |
| 2026-09-13 | 11, 12, 13 | REST 500 assertions are non-vacuous; determinism, tree/validation and Hazelcast storage boundaries gained non-vacuous enforcement | `## 2026-09-13 — Tasks 11, 12 and 13: close the remaining safety-test gaps` |
| 2026-09-09 | 10 | `MutualTlsRestClientsHttpsTest`'s refusal assertion required an `SSLException`, true on Windows but not Linux CI; now accepts either platform's observable, documented in javadoc; verified green on Actions run 34386674901 | `## 2026-09-09 — Task 10: the mTLS refusal assertion made cross-platform` |
| 2026-09-09 | 09 | Shipped defaults fixed to match what's tested (no `EXPOSE_SOURCE_NAMES` by default, named `developer` policy factory); `PiiLogScanTest` parses audit records per-field, closing the word-boundary gap; plan reaches its recommended stopping point | `## 2026-09-09 — Task 09: shipped defaults, and closing the last gap in the log scan` |
| 2026-09-09 | 07 | OAuth2 resource server, Micrometer, PII log scan — closes S8/S9a; real HTTP MCP end-to-end test; a false PII-leak alarm traced to concurrent Maven contention, not a leak | `## 2026-09-09 — Task 07: OAuth2 resource server, Micrometer, PII log scan` |
| 2026-09-09 | 06 | Streamable HTTP transport, per-request caller context, authorisation at the tool — PrivacyContext now derives from an authenticated session | `## 2026-09-09 — Task 06: streamable HTTP transport, per-request caller context, authorisation at the tool` |
| 2026-09-09 | 08 | Three inert controls made to run: purpose validation composed into ScopeResolver, mTLS and reserved-argument tests made able to fail | `## 2026-09-09 — S8: three inert controls made to run` |
| 2026-09-09 | 03, 04, 05 | S8 wave 2: security module (authz/purpose/scope), real principal in audit, Hazelcast identity metrics | `## 2026-09-09 — S8 wave 2: security module, real principal, and identity metrics` |
| 2026-09-09 | 01, 02 | S8 wave 1: InvestigationContext/Capability/metrics SPI, mTLS to sources | `## 2026-09-09 — S8 wave 1: session types, metrics SPI, and mTLS to sources` |
| 2026-09-09 | S7 | Embedded Hazelcast: identity cache fails open, read budget fails closed, scope purge | `## 2026-09-09 — S7 embedded Hazelcast for distributed scope state` |
| 2026-09-09 | S6 | Correlation before scrubbing; findings name which sources agree, never what they hold | `## 2026-09-09 — S6 correlation and consistency findings` |
| 2026-09-09 | S5 | Virtual-thread fan-out with bulkhead, timeout, breaker; REST connectors; request limits | `## 2026-09-09 — S5 parallel connectors and request limits` |
| 2026-09-08 | S4 | Pattern detection plus the scope-aware allowlist that stops it refusing our own output | `## 2026-09-08 — S4 pattern detection with a scope-aware allowlist` |
| 2026-09-08 | S2a | Multi-key resolution so a key rotation does not invalidate running scopes | `## 2026-09-08 — S2a key rotation` |
| 2026-09-08 | S3 | Nested descent (closed a fail-open hole), class-level defaults, descriptors, full action set | `## 2026-09-08 — S3 scrubbing engine, and the nesting hole it closed` |
| 2026-09-08 | S2 | Configurable multi-locale name pools, content-pinned; Unicode canonicalisation | `## 2026-09-08 — S2 configurable multi-locale name pools` |
| 2026-09-08 | S1 | Policy profiles decide actions; class/accessor metadata; processor fails build on unclassified fields | `## 2026-09-08 — S1 policy layer and build-time fail-closed` |
| 2026-09-08 | S0 | Walking skeleton: 8 modules, MCP stdio, HMAC pseudonyms, fail-closed, 31 tests | `## 2026-09-08 — S0 walking skeleton` |
