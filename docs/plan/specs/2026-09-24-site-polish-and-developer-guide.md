# Site polish, concise changelog, developer guide and diagrams — agreed design, 2026-09-24

This is the owner-approved design for the next round of work on the live docs site,
https://aindriub.github.io/data-prism/. It is the input to `/plan`; the planner turns it
into task files. Nothing here is a task file.

## Decisions (binding)

| | Decision |
|---|---|
| Look | Restrained polish. The site reads as a serious infrastructure tool. It gets an identity, but no marketing gloss, no animation and no JavaScript. |
| Changelog | Collapse, don't rewrite. `CHANGELOG.md` stays the only source and is not edited for this work. The site only changes how it is presented. |
| Developer guide snippets | Pulled from compiled code. Tutorial snippets come from marked regions in real, compiled and tested source files. They are never hand-copied into Markdown. |
| Tutorial order | Simple to advanced: (1) write a data-source adapter, then (2) a custom identity resolver. Classifying models in depth and custom audit sinks come in a later plan. |
| Diagrams | Diagrams go where they explain something, embedded in the existing page that covers that concept, each with a short introduction and alt text. There is **no** separate "learn"/diagrams landing page, and nothing is modelled on or named after another project's site (the owner decided this on 2026-09-24; we cite other tools in `docs/comparison.md`, and a lookalike page would reflect badly). The extension-points diagram also appears in the developer guide. |
| Diagram rendering | Mermaid source is committed. SVGs are pre-rendered by a reproducible script (mermaid-cli, via a pinned Docker image) and committed. **Do not** use Material's runtime Mermaid, which loads `mermaid.js` from a third-party CDN. The site must keep loading no third-party scripts or fonts. |
| Honesty | Unchanged from the discoverability spec (`docs/plan/specs/2026-09-23-discoverability.md`). Nothing claims more than the code does. Audit wording follows `docs/audit.md`. Pseudonymisation is never called anonymisation. Every box and arrow in a diagram, and every tutorial step, traces to code or to a verified doc. |

## Facts the planner must respect (from recon, 2026-09-24)

- **The site today.**
  - MkDocs Material 9.7.7, pinned in `docs-site/requirements.txt`.
  - `mkdocs.yml` sets `theme.font: false` and has no `primary`/`accent`, logo, favicon or `extra_css`.
  - `docs-site/overrides/main.html` only injects OG/Twitter/JSON-LD meta.
  - `docs/index.md` is front matter, an include-markdown of the README's `site-intro` block, and a "Where to go next" list.
  - `docs/assets/social-card.png` is the only image, reproducible from `docs-site/social-card/`.
- **Guards that must keep passing.** They live in `docs-site/hooks/site.py`, `docs-site/hooks/check_site.py` and `.github/workflows/pages.yml`:
  - `mkdocs build --strict`
  - page-meta completeness and uniqueness
  - the sitemap equals the real nav
  - no fonts or analytics
  - the llms leak check
  - link existence for links that leave `docs/`
  - `lychee --offline`
  - actionlint

  New pages need a description, either from front matter (new files) or from `docs-site/page-meta.yml` (existing docs, which get no front matter). Existing docs keep their paths: `DataPrismConfigurationFailureAnalyzer` hardcodes `docs/configuration.md` and `docs/quickstart.md`.
- **Changelog.**
  - `CHANGELOG.md` is 287 lines with 5 version headings: `[Unreleased]`, `[0.3.0]` (lines 10–184, about 175 lines), `[0.2.0]`, `[0.1.1]` and `[0.1.0]`.
  - `docs/changelog.md` is `{% include-markdown "../CHANGELOG.md" %}`.
- **Developer material that exists.** `docs/extending.md` has 671 lines and 32 fences. It covers `DataSourceAdapter`, `IdentityResolver`, `@LlmExposedModel` classification, the pom shape and loading with `LOADER_PATH`, and it remains the full reference. None of its snippets are verified against code today.
- **Compiled, tested example code.**
  - `data-prism-quickstart-extension` is a reactor module with `QuickstartSmokeIT`.
  - Its classes are `QuickstartExtensionAutoConfiguration`, `CustomerModel` and `QuickstartCustomerAdapter`.
  - There is no example `IdentityResolver` implementation yet.
- **Public SPIs.**
  - `data-prism-core/.../core/DataSourceAdapter.java`
  - `.../core/IdentityResolver.java`
  - `.../core/SecretKeyProvider.java`
  - `.../audit/AuditSink.java`
  - the annotations under `data-prism-annotations/.../annotations/`
- **Existing markdown extensions.** `admonition`, `pymdownx.details`, `pymdownx.superfences` and `tables`. `pymdownx.snippets`, `attr_list` and `md_in_html` are not enabled yet.

## A. Look: restrained polish

- **Logo and favicon.** A simple, legible SVG mark: a prism or refracted-light motif, restrained, legible at 16 px. It is generated by a small script beside `docs-site/social-card/` so it can be reproduced. Wire it through `theme.logo` and `theme.favicon`.
- **Palette.** A slate or deep-indigo primary with one accent, in both light and dark schemes, with WCAG AA contrast for body text and links. Keep the scheme toggle.
- **Landing page (`docs/index.md`).**
  - A one-line hero with tagline T, and two buttons: Quickstart and Developer guide.
  - Three cards: *Pseudonymise per scope*, *Fail closed*, *Verifiable audit trail*. Each card links to the doc that proves it, and each card's text uses only wording already verified on those pages (T, D, `docs/audit.md`).
  - Then the existing README include and "Where to go next".
  - Uses Material grid cards (`attr_list` + `md_in_html`).
- **One small `extra.css`.** Tables, code blocks and cards only. No web fonts, no JavaScript, no animation.
- **Must keep passing:** every existing guard, including no fonts or analytics, and the home page meta description equal to T.

## B. Changelog: collapse, don't rewrite

- A build hook in `docs-site/hooks/` transforms only the changelog page, at `on_page_markdown` for `changelog.md`:
  - each release becomes a collapsible block (`pymdownx.details`, `???`/`???+`)
  - the most recent release starts open (`???+`); every other release starts closed
  - each summary line shows the version, the date and a short count taken from the entry, such as "N behavioural changes"
  - `[Unreleased]` stays open if it has content, and is dropped from the page if it is empty
- `CHANGELOG.md` is not edited. The GitHub view is unchanged.
- **New guard.** The rendered changelog page contains every `## [x.y.z]` heading from `CHANGELOG.md`, and every link-reference definition still resolves. Prove it can fail: delete a release from a scratch copy of the rendered markdown, and the check must fail.

## C. Developer guide: a new nav section

- **Overview page.** The extension points (`DataSourceAdapter`, `IdentityResolver`, and later `AuditSink` and the classification annotations), what each one is for, and the learning path. It shows diagram D6.
- **Tutorial 1, write a data-source adapter.** From an empty module to a pseudonymised MCP tool call:
  - model and annotations
  - `DataSourceAdapter` implementation
  - auto-configuration
  - pom shape
  - loading it with `LOADER_PATH`
  - `dataprism.sources.<name>`
  - running it and seeing the pseudonymised response

  Snippets come from marked regions in `data-prism-quickstart-extension` sources via `pymdownx.snippets` (`--8<--` with section markers), with `check_paths: true` so a missing file or region fails the build. The region markers are added as comments in the Java files, and adding them must not change behaviour: the module still compiles and `QuickstartSmokeIT` still passes. Any step the tutorial asks a reader to run is run by the tester literally, and its output is quoted from a real run.
- **Tutorial 2, custom identity resolver** (wave 2, after tutorial 1). How one subject is recognised across sources, and how `pass-through` differs.
  - Needs a small example `IdentityResolver`, with a test, in an owned location. Prefer inside `data-prism-quickstart-extension` if that fits its purpose; otherwise use a new example module wired into the reactor.
  - Snippets come from that compiled, tested code.
- **`docs/extending.md` stays** as the full reference. The tutorials link to it, and it gets a short pointer to the guide. Do not duplicate its content.

## D. Diagrams, embedded in context

Each diagram has:
- Mermaid source in `docs-site/diagrams/*.mmd`
- a committed rendered SVG in `docs/assets/diagrams/*.svg`, produced by a script (`docs-site/diagrams/render.sh`) that runs a pinned `minlag/mermaid-cli` image, or equivalent, in Docker
- alt text that states what it shows

Every node and edge must trace to code or to `docs/architecture.md` / `docs/tools.md` / `docs/audit.md`, and the reviewer traces each one. Each diagram is embedded where it explains something, with a two- or three-sentence introduction:

1. **System overview**, in `docs/architecture.md` near the components or "how they talk" section. MCP client → authentication and caller-derived scope and purpose → tool authorisation → orchestrator → reviewed source adapters → enterprise APIs. The response path runs through classification and scrubbing (pseudonymise, redact or remove), then the raw-value leak check, then audit, then the MCP response. Show that no path from an adapter to the MCP layer bypasses the privacy engine (`CLAUDE.md` rule 1; the `docs/architecture.md` boundaries).
2. **One tool call, step by step**, in `docs/tools.md` beside `get_entity_context`. A sequence diagram of one call.
3. **How a pseudonym is made**, in `docs/tools.md` near the pseudonym and scope-isolation material. (scope, subject, namespace, algorithm version) plus the HMAC key → digest → synthetic identity plus discriminator. Scope is `case:` + case id, so a different case gives a different pseudonym.
4. **Fail-closed field decisions**, in `docs/tools.md` or `docs/configuration.md`, wherever unclassified handling is described.
   - A classified field becomes pseudonymised, redacted or removed according to its classification.
   - An unclassified field refuses the whole response, which is the shipped profiles' `FAIL_REQUEST`.
   - A detected identifier shape refuses the response.
   - Do not show relaxed profile settings as reachable. Configuration cannot select them today (see task 84).
5. **The audit chain**, in `docs/audit.md`.
   - One hash chain per writer boot (`<writer-id>/<uuid>`), starting at GENESIS.
   - The verifier detects an edit or deletion inside a chain, including the last record.
   - It cannot detect tail truncation, deletion of a whole boot's records, or recomputation by someone with write access.
   - Wording is strictly `docs/audit.md`'s.
6. **Extension points**, in the developer guide overview. Where a `DataSourceAdapter` and an `IdentityResolver` plug in, relative to the privacy engine.

Embedding a diagram in an existing doc must not move or reword that doc's existing text, beyond one introductory sentence. PLAN.md and other docs cite line numbers, so insertions should go at section ends where possible, and any cited line ranges that shift must be noted for the scribe.

## Waves (the planner turns these into tasks with disjoint Owns)

- **Wave 1 (parallel):**
  - A, the look (logo and favicon script, palette, landing page, `extra.css`, `mkdocs.yml` theme keys)
  - B, the changelog hook and its guard
  - D, the diagrams embedded in `docs/architecture.md`, `docs/tools.md` and `docs/audit.md` (diagram 6 is placed later, by C2 or C1)
  - C1, the developer guide overview and tutorial 1, including snippet markers in the quickstart-extension sources

  `mkdocs.yml`, `docs-site/page-meta.yml`, `docs-site/hooks/check_site.py` and `docs-site/requirements.txt` are shared hot spots. The planner must give each an owner, or sequence the tasks, so no two parallel tasks edit the same file. For example, one task owns `mkdocs.yml` and the others ask for keys through their task files, or the nav and extension changes are split sensibly.
- **Wave 2:** C2, the identity-resolver tutorial and its example code.
- **Later plan, not now:** classifying models in depth; custom audit sinks.

## Acceptance themes for every task

- `mkdocs build --strict` passes, and every existing guard in `check_site.py` passes.
- Each new guard is proven to fail on a planted fault in a scratch copy.
- `pages.yml` passes on the PR: build, lychee and actionlint.
- No third-party script, font or analytics: `grep -rE 'fonts.googleapis|googletagmanager|google-analytics|gtag|unpkg|jsdelivr|cdnjs' site/` is empty.
- Every new page has a unique description of at most 155 characters.
- The honesty greps are empty or negations only.
- Tutorials: every command run literally by the tester, with output quoted from a real run.
- Java changes: `mvn -B verify` for the affected modules is green, and `QuickstartSmokeIT` passes.
