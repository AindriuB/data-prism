# 70 — Cut version 0.3.0 across the reactor

**Repo:** .
**Depends on:** 61, 62, 63, 65, 66, 67, 69 — and lands after 55 and 59
**Owns:**
- pom.xml and every `*/pom.xml` (version literals only)
- server.json
- CHANGELOG.md
- docker/**
- .github/workflows/publish-image.yml (version literals only)
- docs/tools.md
- every remaining `0.2.0` literal in tracked files, wherever it appears — including
  in files another task owns, which is why this task runs after all of them

## Goal
Bump the reactor to 0.3.0, write the 0.3.0 changelog entry from the diffs that
actually landed, and leave no stale version literal anywhere a release attempt
would trip over it. Task 45's release cut established the checklist; follow it.

## Context
- `docs/plan/HISTORY.md` — grep `## 2026-09-17 — Task 45: version 0.2.0 cut, and
  a sweep that had been running blind to `.github/`` — for the full checklist: 19
  module poms plus root, `server.json` (both the `version` field and the OCI
  `identifier` tag), the two MCP `serverInfo` literals, four Dockerfiles,
  `README.md`, three PackagingIT/SmokeIT tests, and `.github/` — which every
  earlier sweep missed because `rg` skipped it without `--hidden`.
- `docs/plan/HISTORY.md` — grep `## 2026-09-17 — 0.2.0 release complete` — for the
  `server.json` shape the registry actually accepts (`registryBaseUrl` and a
  per-package `version` are both rejected) and the tag-move precedent.
- `CHANGELOG.md:8-30` — the `[Unreleased]` section to promote.
- `docs/plan/tasks/55-publish-quickstart-images.md` — verified and held; it merges
  as part of this release once a `v0.3.0` tag exists and `publish-image.yml` has
  been dispatched for the four quickstart images.

## Acceptance
- [ ] `rg --hidden -n '0\.2\.0'` over tracked files (excluding `docs/plan/` and
      `docs/plan/tasks/retired/`, which are history) returns only deliberate
      historical references — each one named in the commit body with why it stays.
- [ ] Root and every module pom read `0.3.0`; `mvn -q -DskipTests package` builds
      the reactor and produces jars whose filenames carry `0.3.0`.
- [ ] `server.json`'s `version` and its OCI package `identifier` tag both read
      `0.3.0`, and the file still carries no `registryBaseUrl` and no per-package
      `version` field.
- [ ] Both MCP `serverInfo` version literals read `0.3.0`, asserted by the existing
      tests that pin them.
- [ ] The four Dockerfiles and `publish-image.yml`'s `workflow_dispatch` default
      read `0.3.0`.
- [ ] `CHANGELOG.md` gains a `## [0.3.0]` entry written from the merged diffs of
      tasks 60-69, not from the task files' intentions. It names: one level of
      named nested catalogues in the configuration-driven connector and the new
      deeper-than-declared refusal code; the durable `FileAuditSink` behind
      `dataprism.audit.sink: hash-chained` with no rotation; the offline chain
      verifier and its stated inability to detect truncation of the most recent
      records; the audit-chain write-ordering fix; the widened bean-classification
      sweep; and the restored reviewed-adapter allow-list.
- [ ] No `CHANGELOG.md` sentence claims the audit log is tamper-proof, immutable or
      independently complete.
- [ ] The full reactor suite passes on the bumped tree, and the test count is
      recorded in the commit body against the commit it was measured on.

## Out of scope
- Publishing: pushing the `v0.3.0` tag, dispatching `publish-image.yml`, the Maven
  Central and MCP registry publishes, and merging task 55 are all release
  operations done after this merges, not part of this diff.
- Any behavioural change. If a version bump reddens a test, fix the literal, not
  the behaviour.
- `docs/quickstart.md`, `docs/configuration.md` and `README.md` prose — task 59
  owns those files; this task changes only version literals inside them, after 59
  has landed.

## Attempt 1 — failed on review (2026-09-23)

Tester: PASS. Build and version bump are sound — keep them:
- Full-reactor `clean verify` against a FRESH isolated local repo is green:
  19 modules, 646 tests.
- `-Prelease` attaches sources and javadoc, then stops at `maven-gpg-plugin:sign`
  (no key in the sandbox — expected).
- `data-prism-server-0.3.0.jar` reports `serverInfo` version `0.3.0` over a
  real MCP `initialize`.
- The distribution image and all four quickstart images build clean.
- `server.json` validates against the live 2025-12-11 schema.
- The only remaining `0.2.0` literals are deliberate history (CHANGELOG's own
  `[0.2.0]` section, `docs/tools.md:373-374`).
- The README bump is allowed by this task's own clause.
- The empty commit 3bc82f6 recording the test count is a legitimate way to
  cite the commit a count was measured on.

Reviewer: CHANGES. The CHANGELOG is public and permanent, so every line must
trace to a merged diff and claim no more than the code does. WHAT FAILED:

1. MISSING entries. Check each against `docs/plan/HISTORY.md` (grep the task
   number):
   - **71**, breaking. The pseudonym discriminator is now 40 bits / 8 chars
     and `ADDRESS` gains one, so every pseudonym changes. `PseudonymisationVersion`
     rejects HmacMD5/HmacSHA1 with `pseudonymisation.algorithm-digest-too-short`.
     Owed per PLAN.md, v0.3.0 section.
   - **75**, breaking for anything keyed on the old value. `instanceId` changes
     from the writer-id to `<writer-id>/<uuid>`. New `INVALID_AUDIT_WRITER`
     refuses a writer-id containing `/`. Owed per PLAN.md.
   - **73**. `approved-sink` with no bean now refuses as
     `AUDIT_SINK_BEAN_REQUIRED`, not `MISSING_AUDIT_SINK`. Breaking for
     anything keyed on the old code.
   - **74**. A sink failure no longer passes its raw message to the MCP client;
     the client gets `AUDIT_UNAVAILABLE`.
   - **Onboarding tasks merged this cycle** — confirm each against HISTORY.md
     before writing it, and omit any that already appear under an earlier
     release:
     - 53: `dataprism.identity.resolver: pass-through`
     - 54: single-statement transport
     - 57: the configuration FailureAnalyzer
     - 58: the no-Java walkthrough
     - 55: `docker compose up` pulls published quickstart images
2. FALSE: the verifier bullet says truncation "exits 3 rather than 0".
   Truncating at a record boundary verifies intact, exit 0 (`docs/audit.md`).
   Exit 3 is only a final record with no terminating newline
   (`AuditChainVerifierCli.java:36-37`). The entry must also state that
   deleting all of one boot's records is undetectable.
3. FALSE: the nested bullet says nested fields use "the same three-shape
   vocabulary flat catalogues already have". Nested leaves refuse
   `identifier: true` (and `nested:`); only `nonSensitive` and classified
   leaves are allowed.
4. FALSE in 0.3.0: a `### Changed` bullet carried over from Unreleased says
   "descendable() always returns false, so a nested object is never covered".
   It now returns `nestedByToken.containsKey(type)`
   (`ConfiguredJsonFieldMetadataResolver.java:110-112`), which contradicts
   this entry's own Added section. Rewrite or fold it so the 0.3.0 entry
   describes 0.3.0's behaviour.
5. OVERCLAIM: the verifier bullet says it "distinguishes several ordinary,
   non-tampering failure modes ... from genuine tampering". It cannot: for
   example, a head deletion preceded by one junk line reports exit 4
   structural. Say tampering "cannot be ruled out" for those shapes.
6. `### Not changed`: say what tamper-evidence covers — an edit or deletion
   inside one writer's chain — and that tail truncation and whole-boot
   deletion are not covered. Do not rely on "against an outside forger"
   alone.
7. Commit-body criterion NOT MET. Name each remaining `0.2.0` literal and why
   it stays. The body says "19 module poms" but it is 18 plus the root; fix
   that too.
8. Minor, take them:
   - The task-69 bullet cites "task 54's earlier relaxation", an internal
     number no public entry mentions. Describe the relaxation itself.
   - `server.json` lists `hash-chained` as an allowed sink but has no
     `DATAPRISM_AUDIT_FILE_PATH` variable, which that sink requires. Add it,
     consistent with how the other environment variables are declared.
   - The reviewer queried 646 tests against a 1018 figure recorded earlier.
     The fresh-repo tester reproduced 646. State in the verification commit
     how the count was taken (surefire + failsafe `Tests run` totals).

When the CHANGELOG is done, re-read it top to bottom as a user would. For every
behavioural claim, name the file or doc that proves it. Remove anything you
cannot name a source for.
