<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>io.github.aindriub</groupId>
    <artifactId>data-prism</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>

  <artifactId>data-prism-quickstart-extension</artifactId>
  <name>Data Prism :: Quickstart :: Reviewed adapter extension</name>
  <description>
    The reviewed DataSourceAdapter and IdentityResolver the Compose quickstart
    (task 18) loads into the packaged standalone server through
    -Dloader.path, per ServerPackagingIT's worked pattern in data-prism-server.
    A plain jar, not a Spring Boot executable: -Dloader.path adds only this
    jar's own classes and resources to the running server's classpath, never
    its dependencies, so everything below other than data-prism-core and
    data-prism-annotations is scoped "provided" — it must already be on
    data-prism-server's own classpath, and spring-boot-starter-web and
    spring-boot-starter-oauth2-resource-server (data-prism-server's own
    dependencies) put it there.
  </description>

  <dependencies>
    <dependency>
      <groupId>io.github.aindriub</groupId>
      <artifactId>data-prism-core</artifactId>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>io.github.aindriub</groupId>
      <artifactId>data-prism-annotations</artifactId>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework</groupId>
      <artifactId>spring-web</artifactId>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-autoconfigure</artifactId>
      <scope>provided</scope>
    </dependency>

    <!-- Test-only, and never a compile/runtime dependency of this module: these
         four entries exist solely so the Maven reactor builds the three
         artefacts this module's Docker-free smoke test starts as subprocesses,
         plus the packaged server it loads this extension into, before this
         module's own test phase runs. See QuickstartSmokeIT. -->
    <dependency>
      <groupId>io.github.aindriub</groupId>
      <artifactId>data-prism-quickstart-fixtures</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>io.github.aindriub</groupId>
      <artifactId>data-prism-quickstart-issuer</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>io.github.aindriub</groupId>
      <artifactId>data-prism-server</artifactId>
      <scope>test</scope>
    </dependency>

    <!-- The MCP SDK's own client transport, for QuickstartSmokeIT to drive the
         packaged server the same way a real agent would, rather than a
         synthetic exchange. Test-only: never shipped in this module's jar. -->
    <dependency>
      <groupId>io.modelcontextprotocol.sdk</groupId>
      <artifactId>mcp-core</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>io.modelcontextprotocol.sdk</groupId>
      <artifactId>mcp-json-jackson2</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <!-- On the processor path, not the compile classpath: this module
               depends on the @LlmExposedModel classification check running,
               not on the checker's own classes. -->
          <annotationProcessorPaths>
            <path>
              <groupId>io.github.aindriub</groupId>
              <artifactId>data-prism-processor</artifactId>
              <version>${project.version}</version>
            </path>
          </annotationProcessorPaths>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-failsafe-plugin</artifactId>
        <version>${surefire.version}</version>
        <executions>
          <execution><goals><goal>integration-test</goal><goal>verify</goal></goals></execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
