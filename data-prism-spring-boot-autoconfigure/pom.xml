<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>io.github.aindriub</groupId>
    <artifactId>data-prism</artifactId>
    <version>0.3.1</version>
  </parent>
  <artifactId>data-prism-spring-boot-autoconfigure</artifactId>
  <name>Data Prism :: Spring Boot auto-configuration</name>
  <description>Validated deployment binding and privacy-pipeline wiring.</description>
  <dependencies>
    <dependency><groupId>io.github.aindriub</groupId><artifactId>data-prism-mcp</artifactId></dependency>
    <dependency><groupId>io.github.aindriub</groupId><artifactId>data-prism-pseudonymisation</artifactId></dependency>
    <dependency><groupId>io.github.aindriub</groupId><artifactId>data-prism-validation</artifactId></dependency>
    <!-- Optional: the shared read budget for topology: embedded. Optional so a
         single-node consumer, which never touches these classes, does not take
         Hazelcast onto its classpath through this starter. -->
    <dependency><groupId>io.github.aindriub</groupId><artifactId>data-prism-hazelcast</artifactId><optional>true</optional></dependency>
    <dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-autoconfigure</artifactId></dependency>
    <dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-validation</artifactId></dependency>
    <dependency><groupId>jakarta.servlet</groupId><artifactId>jakarta.servlet-api</artifactId><version>6.1.0</version><scope>provided</scope></dependency>
    <!-- JwtDecoderSupport and JwtCallerContextExtractor are static-factory /
         plain classes an edge configuration calls, not an auto-configured
         bean — so this stays `provided`, a non-transitive scope. A starter
         user who never runs an HTTP resource server never pulls Spring
         Security onto their classpath through this module. -->
    <dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-oauth2-resource-server</artifactId><scope>provided</scope></dependency>
    <dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-test</artifactId><scope>test</scope></dependency>
    <dependency><groupId>org.springframework</groupId><artifactId>spring-web</artifactId><scope>test</scope></dependency>
  </dependencies>
</project>
